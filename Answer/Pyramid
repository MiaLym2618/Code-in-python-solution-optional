from graphics import Canvas
import random

CANVAS_WIDTH = 600      # Width of drawing canvas in pixels
CANVAS_HEIGHT = 300     # Height of drawing canvas in pixels

BRICK_WIDTH	= 30        # The width of each brick in pixels
BRICK_HEIGHT = 12       # The height of each brick in pixels
BRICKS_IN_BASE = 14     # The number of bricks in the base

WIDTH=((CANVAS_WIDTH/BRICK_WIDTH)-BRICKS_IN_BASE)*0.5*BRICK_WIDTH
HEIGHT=CANVAS_HEIGHT-BRICK_HEIGHT

def main():
    canvas = Canvas(CANVAS_WIDTH, CANVAS_HEIGHT)
    for k in range(0,14):
        for i in range(14-k,0,-1):
            canvas.create_rectangle(
            WIDTH+BRICK_WIDTH*(i+0.5*k), 
            HEIGHT-BRICK_HEIGHT*k, 
            WIDTH+BRICK_WIDTH*(i+0.5*k-1), 
            HEIGHT-BRICK_HEIGHT*(k-1), 
            "yellow", 
            "black"

            )


if __name__ == '__main__':
    main()
